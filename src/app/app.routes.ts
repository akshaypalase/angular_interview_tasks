import { Routes } from '@angular/router';
import { DependentDropdown } from './dependent-dropdown/dependent-dropdown';

export const routes: Routes = [
    {path: 'dependent-dropdown', component: DependentDropdown}
];
